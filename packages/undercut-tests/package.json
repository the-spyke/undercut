{
  "name": "@undercut/tests",
  "version": "1.0.0",
  "description": "Undercut tests",
  "author": "Anton Alexandrenok",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "build": "echo 'Empty build step'",
    "clear": "rm -rf build dist",
    "dist": "echo 'This package doesn't support publishing!' && exit 1",
    "lint": "eslint --format='unix' '**/*.?(c|m)js'",
    "test": "echo 'No tests in this package'"
  },
  "devDependencies": {
    "@jest/globals": "^26.1.0",
    "@undercut/testing": "^1.0.0",
    "@undercut/utils": "*",
    "eslint": "^7.5.0"
  }
}
