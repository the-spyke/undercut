{
  "name": "@undercut/tests",
  "version": "1.0.0",
  "description": "Operation specifict tests as they are shared between pull and push.",
  "author": "Anton Alexandrenok",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "echo 'Empty build step'",
    "clear": "rm -rf build dist",
    "dist": "echo 'This package doesn't support publishing!' && exit 1",
    "lint": "eslint --format='unix' '**/*.@(cjs|mjs|js|ts)'",
    "test": "echo 'No tests in this package'",
    "ts": "tsc --noEmit"
  },
  "devDependencies": {
    "@jest/globals": "^26.6.2",
    "@undercut/testing": "^1.0.0",
    "@undercut/utils": "*",
    "eslint": "^7.14.0",
    "typescript": "^4.1.5"
  }
}
