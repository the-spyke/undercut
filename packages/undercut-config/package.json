{
  "name": "@undercut/config",
  "version": "1.0.0",
  "description": "Undercut build configs",
  "author": "Anton Alexandrenok",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./index.js",
  "scripts": {
    "build": "echo 'Empty build step'",
    "clear": "rm -rf build dist",
    "dist": "echo 'This package doesn't support publishing!' && exit 1",
    "lint": "eslint --format='unix' '**/*.?(c|m)js'",
    "test": "echo 'No tests'"
  },
  "dependencies": {
    "core-js": "*",
    "jest-diff": "^26.6.2",
    "yargs": "^16.2.0"
  },
  "devDependencies": {
    "eslint": "^7.14.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/the-spyke/undercut",
    "directory": "packages/undercut-config"
  }
}
